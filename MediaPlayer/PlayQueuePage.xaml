<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MediaPlayer.PlayQueuePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        x:Name="RootGrid"
        Padding="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Text="Play Queue" 
            FontSize="32" 
            FontWeight="Bold" 
            HorizontalAlignment="Left"
            Margin="0,0,0,10"/>

        <StackPanel 
            Grid.Row="1"
            Orientation="Horizontal" 
            HorizontalAlignment="Left"
            Spacing="20"
            Margin="0,0,0,10">

            <Button 
                x:Name="addMediaButton"
                Click="addMediaButton_Click"
                Content="Add Media"
                Width="100"
                Height="40"/>

            <Button 
                x:Name="clearButton"
                Click="clearButton_Click"
                Content="Clear"
                Width="100"
                Height="40"/>

        </StackPanel>

        <ListView 
            Grid.Row="2"
            x:Name="MediaQueueListView"
            ItemsSource="{x:Bind MediaQueue, Mode=OneWay}"
            SelectionMode="Single"
            SelectionChanged="MediaQueueListView_SelectionChanged"
            Margin="0,10,0,0">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:MediaFileViewModel">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{x:Bind Name}" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Year}" FontSize="14" Foreground="Gray"/>
                        <TextBlock Text="{x:Bind Duration}" FontSize="14"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </Grid>
</Page>
