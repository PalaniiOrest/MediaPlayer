<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MediaPlayer.EffectsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
        <ComboBox x:Name="VideoEffectsComboBox" Width="200">
            <ComboBoxItem IsSelected="True" Content="Video Effects" IsEnabled="False" />
            <CheckBox x:Name="Grayscale" Content="Grayscale" />
            <CheckBox x:Name="Sepia" Content="Sepia" />
            <CheckBox x:Name="InvertColors" Content="Invert Colors" />
        </ComboBox>

        <ComboBox x:Name="AudioEffectsComboBox" Width="200">
            <ComboBoxItem IsSelected="True" Content="Audio Effects" IsEnabled="False" />
            <CheckBox x:Name="Echo" Content="Echo" />
            <CheckBox x:Name="BassBoost" Content="Bass Boost" />
            <CheckBox x:Name="Reverb" Content="Reverb" />
            <CheckBox x:Name="GainHalf" Content="Gain Half" />
        </ComboBox>
    </StackPanel>
</UserControl>
